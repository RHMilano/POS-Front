import { NumeroAletrasPipe } from './numero-aletras.pipe';

describe('NumeroAletrasPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroAletrasPipe();
    expect(pipe).toBeTruthy();
  });
});
