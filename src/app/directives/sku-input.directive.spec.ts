import { SkuInputDirective } from './sku-input.directive';

describe('SkuInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SkuInputDirective();
    expect(directive).toBeTruthy();
  });
});
