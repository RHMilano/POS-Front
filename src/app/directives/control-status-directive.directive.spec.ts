import { ControlStatusDirectiveDirective } from './control-status-directive.directive';

describe('ControlStatusDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlStatusDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
