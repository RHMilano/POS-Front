import { FocusTicketRowDirective } from './focus-ticket-row.directive';

describe('FocusTicketRowDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusTicketRowDirective();
    expect(directive).toBeTruthy();
  });
});
