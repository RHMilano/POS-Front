import { ColorConfigPosDirective } from './color-config-pos.directive';

describe('ColorConfigPosDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorConfigPosDirective();
    expect(directive).toBeTruthy();
  });
});
