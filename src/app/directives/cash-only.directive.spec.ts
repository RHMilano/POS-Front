import { CashOnlyDirective } from './cash-only.directive';

describe('CashOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CashOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
